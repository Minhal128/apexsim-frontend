const fs=require('fs');const p='src/app/dashboard/(trade)/spot-trade/page.tsx';let t=fs.readFileSync(p,'utf8');t=t.replace("const assetSymbol = selectedCoin.symbol.includes('/') ? selectedCoin.symbol.toUpperCase() : `${selectedCoin.symbol.toUpperCase()}/${quoteParam}`;", "const assetSymbol = selectedCoin.symbol.includes('/') || category !== 'crypto' ? selectedCoin.symbol.toUpperCase() : `${selectedCoin.symbol.toUpperCase()}/${quoteParam}`;");fs.writeFileSync(p,t);